export enum IBCService {
  IBC = 'iBootCloud',
  DATABOX = 'Databox',
  MICOFUN = 'Micofun',
  SEARCHABLE = 'Searchable',
  EVENTBURG = 'Eventburg',
  MEDIFY = 'Medify',
}

export enum ENV {
  PRODUCTION,
  TEST,
  DEVELOPMENT,
}
