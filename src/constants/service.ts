export enum IBCService {
  IBC = 'iBootCloud',
  DATABOX = 'Databox',
  MICOFUN = 'Micofun',
  SEARCHABLE = 'Searchable',
  EVENTBURG = 'Eventburg',
}

export enum ENV {
  PRODUCTION,
  TEST,
  DEVELOPMENT,
}
