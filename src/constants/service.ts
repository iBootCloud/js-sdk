export enum IBCService {
  IBC = 'iBootCloud',
  DATABOX = 'Databox',
  MICOFUN = 'Micofun',
  SEARCHABLE = 'Searchable',
  EVENTBURG = 'Eventburg',
  MEDIFY = 'Medify',
  IDENTITY = 'Identity',
}

export enum ENV {
  PRODUCTION,
  TEST,
  DEVELOPMENT,
}
